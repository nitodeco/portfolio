{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "rewrites": [
        {
            "source": "/ingest/:path(.*)",
            "destination": "https://eu.i.posthog.com/:path*"
        }
    ]
}
